@model IEnumerable<Aeropuerto.Models.Piloto>

@{
    ViewData["Title"] = "Pilotos";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.Nombre)</th>
            <th>@Html.DisplayNameFor(model => model.Apellidos)</th>
            <th>@Html.DisplayNameFor(model => model.Edad)</th>
            <th>@Html.DisplayNameFor(model => model.Aerolinea.Nombre)</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Nombre)</td>
                <td>@Html.DisplayFor(modelItem => item.Apellidos)</td>
                <td>@Html.DisplayFor(modelItem => item.Edad)</td>
                <td>@Html.DisplayFor(modelItem => item.Aerolinea.Nombre)</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.PilotoId" class="btn btn-warning">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.PilotoId" class="btn btn-info">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.PilotoId" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
