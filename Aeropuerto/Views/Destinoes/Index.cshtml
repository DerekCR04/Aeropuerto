@model IEnumerable<Aeropuerto.Models.Destino>

@{
    ViewData["Title"] = "Destinos";
}

<h1>@ViewData["Title"]</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(model => model.NombreCiudad)</th>
            <th>@Html.DisplayNameFor(model => model.Pais)</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.NombreCiudad)</td>
                <td>@Html.DisplayFor(modelItem => item.Pais)</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.DestinoId" class="btn btn-warning">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.DestinoId" class="btn btn-info">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.DestinoId" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
