@model IEnumerable<Aeropuerto.Models.Avione>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Modelo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Capacidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aerolinea)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Modelo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Capacidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aerolinea.AerolineaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AvionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AvionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AvionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
